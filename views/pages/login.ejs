<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head'); %>
  </head>
  <body>
    <header><%- include('../partials/header'); %></header>
    <!--<%
if (window.location.href.indexOf('from=reg') > -1){
  <span>You have received an email, please validate your email address by clicking on the link in the email</span>
}
%> -->
    <div class="container login">
      <div id="register">
        <form id="registerForm">
          <span id="errorMessage"></span>
          <label for="email">E-mail:</label>
          <input type="text" name="email" id="logInEmail" />
          <label for="password">Jelszo:</label>
          <input type="password" name="password" id="logInPassword" /><br />
          <button id="logInButton" type="submit">Belepek</button>
          <button
            id="toRegButton"
            onclick="window.location.href='/register'; return false;"
          >
            Regisztralok
          </button>
        </form>
      </div>
    </div>

    <script src="/javascript/nav.js"></script>
  </body>
</html>
